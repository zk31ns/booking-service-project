# 🤝 CONTRIBUTING.md

## Руководство для разработчиков

Спасибо за интерес к проекту! Этот документ содержит правила и процессы разработки для всей команды.

---

## 📋 Оглавление

1. [Структура разработки](#структура-разработки)
2. [Git Workflow](#git-workflow)
3. [Правила создания веток](#правила-создания-веток)
4. [Pull Request Process](#pull-request-process)
5. [Стилистика кода](#стилистика-кода)
6. [Работа с миграциями](#работа-с-миграциями)
7. [Правила проекта](#правила-проекта)
8. [Запрещено](#запрещено)
9. [FAQ](#faq)

---

## Структура разработки

### Роли в команде

| Роль | Ответственность |
|------|-----------------|
| **Тимлид (Данил)** | Ревью PR, merge в develop, создание миграций |
| **Разработчик** | Создание feature, фиксы баков, написание кода |
| **Code Reviewer** | Проверка качества кода, стилистики |

### Основные ветки

- **`develop`** — главная ветка разработки (всегда стабильна)
- **`main` / `master`** — production-ready код (редко обновляется)
- **`feature/*`** — новые функции
- **`fix/*`** — исправления багов
- **`refactor/*`** — рефакторинг кода

---

## Git Workflow

### Базовый процесс

```bash
# 1. Обновить локальный develop
git checkout develop
git pull origin develop

# 2. Создать feature ветку от develop
git checkout -b feature/описание-функции

# 3. Сделать комиты с понятными сообщениями
git commit -m "feat: добавлена функция X"

# 4. Запушить в origin
git push origin feature/описание-функции

# 5. Создать Pull Request (PR) на GitHub
# (описание заполнить по шаблону)

# 6. После одобрения тимлида → merge в develop
# Удалить feature ветку после merge
```

---

## Правила создания веток

### Формат имени ветки

```
<тип>/<краткое-описание>
```

### Типы веток

| Тип | Пример | Когда использовать |
|-----|--------|-------------------|
| `feature/` | `feature/user-authentication` | Новая функция |
| `fix/` | `fix/booking-validation` | Исправление бага |
| `refactor/` | `refactor/database-schema` | Улучшение кода |
| `docs/` | `docs/api-documentation` | Документация |
| `chore/` | `chore/update-dependencies` | Обновления зависимостей |

### ✅ Хорошие примеры

```
feature/user-registration
feature/add-booking-slots
fix/null-pointer-exception
refactor/simplify-auth-service
docs/update-readme
```

### ❌ Плохие примеры

```
feature1
fix-bug
update-code
my-branch
test
```

---

## Pull Request Process

### Требования к PR

1. **Название PR**
   - Использовать формат: `[TYPE] Краткое описание`
   - Пример: `[FEATURE] Добавлена аутентификация пользователей`

2. **Описание PR**
   - Заполнить по [pull request template](.github/pull_request_template.md)
   - Объяснить что и зачем было изменено
   - Упомянуть связанные issues (если есть)

3. **Код должен:**
   - Пройти все проверки (pre-commit, tests)
   - Соответствовать стилистике проекта
   - Иметь type hints для всех функций
   - Содержать docstring для публичных методов

4. **Коммиты**
   - Атомарные, логически завершенные
   - С понятными сообщениями на русском или английском
   - Минимум 1 коммит, максимум по смыслу

### Процесс review

```
PR created → CI checks → Code review → Approved → Merge → Deploy
```

**Статусы PR:**
- 🔴 `request changes` — требуются исправления
- 🟡 `review in progress` — идет ревью
- 🟢 `approved` — одобрено, готово к merge

---

## Стилистика кода

### Правила проекта

| Правило | Значение |
|---------|----------|
| Line Length | 79 символов |
| Indent | 4 пробела |
| Quotes | Single quotes (`'`) |
| Line Endings | LF (Unix) |
| Python Target | 3.11+ |
| Formatter | `ruff format` |
| Linter | `ruff check` |

### Проверка стилистики

```bash
# Проверить все файлы
ruff check .

# Автоматически исправить
ruff check . --fix
ruff format .

# Запустить все pre-commit checks
pre-commit run --all-files
```

### Type Hints (обязательны!)

```python
# ❌ Неправильно
def create_user(name, email):
    pass

# ✅ Правильно
def create_user(name: str, email: str) -> User:
    '''Создать пользователя.

    Args:
        name: Имя пользователя
        email: Email пользователя

    Returns:
        Объект User
    '''
    pass
```

### Импорты

```python
# ✅ Правильный порядок импортов
# 1. Стандартная библиотека
from datetime import datetime

# 2. Третьей стороны
from pydantic import BaseModel

# 3. Локальные импорты из проекта
from src.app.models.user import User
```

### Назначение переменных

```python
# ✅ Константы
API_VERSION = '1.0'
MAX_USERS = 100
TIMEOUT_SECONDS = 30

# ✅ Переменные
user_name = 'John'
is_active = True
total_count = 0

# ✅ Приватные методы
def _internal_helper():
    pass
```

---

## Работа с миграциями

### ⚠️ ВАЖНО: Только тимлид создает миграции!

### Процесс

**Разработчик (вы):**
1. Создать/изменить модель в `src/app/models/`
2. Закомитить в feature ветку
3. Создать PR

**Тимлид (Данил):**
1. Ревью PR + merge в develop
2. Создать миграцию: `alembic revision --autogenerate -m "описание"`
3. Проверить миграцию в `src/app/alembic/versions/`
4. Закомитить миграцию в develop
5. Запушить в origin

### Как НЕ делать

```bash
# ❌ НЕ создавайте миграции самостоятельно!
alembic revision --autogenerate -m "my_migration"

# ❌ НЕ редактируйте старые миграции!

# ❌ НЕ коммитьте multiple head revisions!
```

### Как применить миграции

```bash
# Применить все миграции до последней
alembic upgrade head

# Применить конкретную версию
alembic upgrade ae1027a6acf

# Откатить на шаг назад
alembic downgrade -1

# Посмотреть текущую версию
alembic current
```

---

## Правила проекта

### ✅ Обязательно

- [ ] Использовать абсолютные импорты: `from src.app.*`
- [ ] Писать type hints для всех функций
- [ ] Использовать async/await для асинхронного кода
- [ ] Добавлять docstring для публичных методов
- [ ] Запускать pre-commit перед каждым коммитом
- [ ] Заполнять .env из .env.example
- [ ] Делать атомарные коммиты

### ✅ Рекомендуется

- Использовать логирование через `loguru`
- Добавлять примеры использования в docstring
- Писать тесты для новых функций
- Обновлять документацию вместе с кодом

---

## Запрещено

### ❌ Архитектура

- Использовать `from app.*` вместо `from src.app.*`
- Создавать циклические импорты
- Смешивать async и sync код в одной функции
- Сохранять credentials в исходниках (используйте .env)

### ❌ API

- Использовать deprecated `@app.on_event()` (используйте `lifespan`)
- Возвращать `dict` вместо Pydantic моделей
- Забывать type hints в функциях
- Создавать очень длинные функции (>50 строк)

### ❌ Базы данных

- Создавать миграции самостоятельно (только тимлид!)
- Редактировать старые миграции
- Использовать raw SQL вместо SQLAlchemy ORM
- Коммитить файлы миграций без одобрения тимлида

### ❌ Git

- Коммитить `.env` файлы
- Коммитить IDE-specific файлы (`.vscode/`, `.idea/` в .gitignore)
- Делать очень большие PR (>500 строк - разбить на несколько)
- Коммитить непроверенный код

---

## FAQ

### Q: Как установить pre-commit?

```bash
# Активировать pre-commit для этого репозитория
pre-commit install

# Проверить все файлы один раз
pre-commit run --all-files

# Если pre-commit найдет ошибки и их исправит:
git add .
git commit -m "fix: auto-fixed style issues"
```

### Q: Что делать если забыл про .env?

```bash
# Скопировать шаблон
cp .env.example .env

# Заполнить локальные значения (спросить тимлида если не уверен)
# Не коммитить этот файл!
```

### Q: Как узнать правильный формат коммита?

```bash
# Посмотреть последние коммиты
git log --oneline

# Примеры:
# feat: add user authentication
# fix: resolve null pointer in booking service
# refactor: simplify database queries
# docs: update README with setup instructions
# test: add tests for user registration
```

### Q: Могу ли я работать в main/develop?

❌ **НЕТ!** Всегда создавайте feature ветку:

```bash
# ✅ Правильно
git checkout -b feature/your-feature
git commit -m "feat: your changes"

# ❌ Неправильно
git checkout develop
git commit -m "changed something"
```

### Q: PR не проходит проверки, что делать?

```bash
# 1. Посмотреть ошибки в GitHub Actions
# 2. Локально запустить те же проверки
ruff check .
pre-commit run --all-files

# 3. Исправить ошибки
# 4. Закомитить исправления
git add .
git commit -m "fix: resolve linting issues"
git push
```

---

## Контакты

- **Тимлид:** Данил (@Mordovin)
- **GitHub:** [Yandex-Practicum-Students/57_58_booking_seats_team_2](https://github.com/Yandex-Practicum-Students/57_58_booking_seats_team_2)
- **Документация:** см. README.md и другие .md файлы

---

## История изменений

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.0 | 20.12.2025 | Первая версия CONTRIBUTING.md |

---

**Спасибо за соблюдение этих правил! Успешной разработки! 🚀**
