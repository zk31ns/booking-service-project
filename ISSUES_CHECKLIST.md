# ISSUES_CHECKLIST.md

## 🧩 ДЕКОМПОЗИЦИЯ ЗАДАЧ ПРОЕКТА

Приоритеты:
- **P1** — критически важно, без этого проект не запустится
- **P2** — основной функционал (MVP)
- **P3** — улучшения, оптимизация
- **P4** — опционально, если останется время

---

## 🟥 P1 — БАЗА ПРОЕКТА, САМЫЕ КРИТИЧНЫЕ ЗАДАЧИ

### P1.1. Настройка структуры проекта
**Ответственный:** Данил  
**Срок:** 1 день  
**Метка:** `P1`, `backend`, `task`

**Описание:**  
Создать стандартную структуру директорий для FastAPI приложения:
- `/src/app/` — основное приложение
- `/src/app/api/v1/` — эндпоинты
- `/src/app/core/` — конфигурация, security, logging
- `/src/app/db/` — подключение к БД
- `/src/app/models/` — ORM модели
- `/src/app/schemas/` — Pydantic схемы
- `/src/app/services/` — бизнес-логика
- `/src/app/repositories/` — CRUD операции
- `/src/app/utils/` — вспомогательные функции
- `/src/alembic/` — миграции БД

**Acceptance Criteria:**
- [x] Все директории созданы и инициализированы (`__init__.py`)
- [x] Структура соответствует ARCHITECTURE.md
- [x] **ЗАВЕРШЕНО** - Полная структура проекта готова

---

### P1.2. Финализация зависимостей (requirements.txt)
**Ответственный:** Данил  
**Срок:** 0.5 дня  
**Метка:** `P1`, `backend`, `task`

**Описание:**  
Проверить и финализировать requirements.txt, убедиться что все зависимости совместимы.

**Acceptance Criteria:**
- [x] requirements.txt актуален и проверен
- [x] Все версии совместимы между собой
- [x] `pip install -r requirements.txt` работает без ошибок
- [x] **ЗАВЕРШЕНО** - requirements.txt в корне, все 40+ пакетов установлены

---

### P1.3. Настройка Alembic и создание Base
**Ответственный:** Данил  
**Срок:** 1 день  
**Метка:** `P1`, `backend`, `task`

**Описание:**  
Инициализировать Alembic для миграций БД:
- Создать `alembic.ini`
- Настроить `alembic/env.py` для async SQLAlchemy
- Создать `Base` класс в `/src/app/db/base.py`
- Создать первую миграцию (структура готова, но таблиц ещё нет)

**Acceptance Criteria:**
- [ ] `alembic init alembic` выполнен
- [ ] `env.py` настроен на async engine
- [ ] Base наследуется от DeclarativeBase
- [ ] `alembic revision --autogenerate -m "init"` работает

---

### P1.4. Настройка конфигурации (pydantic-settings)
**Ответственный:** Данил  
**Срок:** 0.5 дня  
**Метка:** `P1`, `backend`, `task`

**Описание:**  
Создать файл `/src/app/core/config.py` с использованием Pydantic Settings:

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str
    REDIS_URL: str
    RABBITMQ_URL: str
    JWT_SECRET_KEY: str
    JWT_ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 60
    # ... остальные переменные
```

**Acceptance Criteria:**
- [x] Файл `.env.example` существует с примерами всех переменных
- [x] Settings читает переменные из `.env`
- [x] В `.gitignore` добавлен `.env`
- [x] **ЗАВЕРШЕНО** - config.py полностью настроен, все переменные работают

---

### P1.5. Docker и docker-compose
**Ответственный:** Данил  
**Срок:** 1 день  
**Метка:** `P1`, `backend`, `devops`

**Описание:**  
Создать рабочую конфигурацию Docker:
- `Dockerfile` для FastAPI приложения
- `docker-compose.yml` со всеми сервисами:
  - FastAPI (api)
  - PostgreSQL (postgres)
  - Redis (redis)
  - RabbitMQ (rabbitmq)
  - Celery Worker (celery_worker)
  - Celery Beat (celery_beat) — опционально
  - Flower (flower) — опционально

**Acceptance Criteria:**
- [ ] `docker-compose up --build` поднимает все сервисы
- [ ] Postgres доступен на `localhost:5432`
- [ ] Redis доступен на `localhost:6379`
- [ ] RabbitMQ UI доступен на `http://localhost:15672`
- [ ] FastAPI доступен на `http://localhost:8000`

---

### P1.6. Логирование (Loguru)
**Ответственный:** Данил  
**Срок:** 0.5 дня  
**Метка:** `P1`, `backend`, `task`

**Описание:**  
Настроить Loguru в `/src/app/core/logging.py`:
- Логи в консоль
- Логи в файл с ротацией (`logs/app.log`)
- Формат: timestamp | level | function | message

**Acceptance Criteria:**
- [x] Логирование работает при запуске приложения
- [x] Логи пишутся в файл
- [x] Ротация настроена на 10MB
- [x] **ЗАВЕРШЕНО** - Loguru полностью настроен с ротацией и форматированием

---

### P1.7. Реализация Health-check эндпоинта
**Ответственный:** Данил  
**Срок:** 2 часа  
**Метка:** `P1`, `backend`, `feature`

**Описание:**  
Создать эндпоинт для проверки здоровья приложения:

```
GET /api/v1/health
→ 200 OK
{
  "status": "ok",
  "timestamp": "2025-12-11T12:00:00Z",
  "version": "1.0.0"
}
```

**Acceptance Criteria:**
- [ ] Эндпоинт доступен на `GET /api/v1/health`
- [ ] Возвращает 200 и JSON с статусом
- [ ] Проверяет подключение к БД (опционально)
- [x] **ЗАВЕРШЕНО** - Реализовано в `/src/app/api/v1/health.py`

---

## 🟧 P2 — МОДЕЛИ + CRUD + API (основной функционал MVP)

### USERS / AUTH — Ответственный: Александр

#### P2.1. Создать модели User
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Создать ORM модель User в `/src/app/models/user.py`:

```python
class User(Base):
    __tablename__ = "users"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    username: Mapped[str] = mapped_column(String(255), unique=True)
    email: Mapped[str | None]
    phone: Mapped[str | None]
    password_hash: Mapped[str]
    is_blocked: Mapped[bool] = mapped_column(default=False)
    is_superuser: Mapped[bool] = mapped_column(default=False)
    created_at: Mapped[datetime] = mapped_column(server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(onupdate=func.now())
    active: Mapped[bool] = mapped_column(default=True)
```

**Acceptance Criteria:**
- [ ] Модель создана и наследует Base
- [ ] Все поля соответствуют DATABASE_SCHEMA.md
- [ ] Индексы на `username` и `email`

---

#### P2.2. Реализовать JWT авторизацию
**Срок:** 1 день  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Создать JWT токены и эндпоинты авторизации в `/src/app/api/v1/users/`:
- POST `/auth/register` — регистрация
- POST `/auth/login` — логин с username/password
- POST `/auth/refresh` — обновление токена
- GET `/users/me` — получить текущего пользователя

**Acceptance Criteria:**
- [ ] Регистрация работает, пароль хешируется bcrypt
- [ ] Логин возвращает access_token и refresh_token
- [ ] JWT токен содержит user_id в payload
- [ ] Токены истекают по времени

---

#### P2.3. CRUD UserRepository
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Реализовать CRUD операции в `/src/app/repositories/user_repository.py`:
- `create_user()`
- `get_user_by_id()`
- `get_user_by_username()`
- `update_user()`
- `delete_user()` (логическое удаление)

**Acceptance Criteria:**
- [ ] Все методы async
- [ ] Используются mapped_column для типов
- [ ] Тесты на отсутствие SQL injection

---

#### P2.4. UserService
**Срок:** 1 день  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Бизнес-логика пользователей в `/src/app/services/user_service.py`:
- Регистрация (создание пользователя)
- Логин (проверка пароля, генерация токена)
- Обновление профиля
- Блокировка/разблокировка пользователя

**Acceptance Criteria:**
- [ ] Все методы имеют валидацию входных данных
- [ ] Пароли хешируются/проверяются корректно
- [ ] Логирование всех важных действий

---

#### P2.5. UsersRouter
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
API маршруты для пользователей в `/src/app/api/v1/users/router.py`:
- GET `/users/me` — профиль текущего пользователя
- PATCH `/users/me` — обновление профиля

**Acceptance Criteria:**
- [ ] Все эндпоинты требуют авторизацию (JWT)
- [ ] Возвращают 200, 401, 422 коды
- [ ] Документированы в Swagger

---

### CAFES — Ответственный: Павел

#### P2.6. Модель Cafe
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Создать модель Cafe в `/src/app/models/cafe.py`:
- Поля: id, name, address, phone, description, photo_id, timestamps, active

**Acceptance Criteria:**
- [ ] Модель создана
- [ ] Связь с Media (фото)
- [ ] Миграция создана

---

#### P2.7. CafeRepository
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
CRUD операции для Cafe.

**Acceptance Criteria:**
- [ ] `get_all()`, `get_by_id()`, `create()`, `update()`, `delete()`

---

#### P2.8. CafeService
**Срок:** 1 день  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Бизнес-логика для кафе.

**Acceptance Criteria:**
- [ ] Валидация имени и адреса
- [ ] Загрузка/обновление фото

---

#### P2.9. CafeRouter
**Срок:** 1 день  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
API маршруты:
- GET `/cafes` — список всех кафе
- GET `/cafes/{id}` — детали кафе
- POST `/cafes` — создание кафе (админ)
- PATCH `/cafes/{id}` — обновление
- DELETE `/cafes/{id}` — удаление (логическое)

**Acceptance Criteria:**
- [ ] Пагинация на списке кафе
- [ ] Фильтр по активности
- [ ] Возвращает связанные данные (столики, слоты)

---

### TABLES — Ответственный: Павел

#### P2.10. Модель Table
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Модель Table в `/src/app/models/table.py`:
- cafe_id (FK), seats, description, timestamps, active

**Acceptance Criteria:**
- [ ] Связь с Cafe (many-to-one)
- [ ] Индекс на (cafe_id, id)

---

#### P2.11. TableRepository
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
CRUD для столиков.

---

#### P2.12. TableService
**Срок:** 1 день  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Бизнес-логика: валидация seats > 0, проверка кафе существует.

---

#### P2.13. TableRouter
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
API маршруты:
- GET `/cafes/{cafe_id}/tables`
- POST `/cafes/{cafe_id}/tables`
- PATCH `/cafes/{cafe_id}/tables/{table_id}`
- DELETE `/cafes/{cafe_id}/tables/{table_id}`

---

### SLOTS — Ответственный: Лев

#### P2.14. Модель Slot
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Модель Slot в `/src/app/models/slot.py`:
- cafe_id (FK), start_time, end_time, timestamps, active
- Уникальный индекс на (cafe_id, start_time, end_time)

**Acceptance Criteria:**
- [ ] start_time < end_time (CHECK constraint)
- [ ] Типы: time, не datetime

---

#### P2.15. SlotRepository
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
CRUD для слотов.

---

#### P2.16. SlotService (валидация пересечений)
**Срок:** 1 день  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Бизнес-логика:
- Валидация start_time < end_time
- Проверка что слот не пересекается с другими в этом кафе
- Проверка что кафе существует и активно

**Acceptance Criteria:**
- [ ] Нельзя создать пересекающиеся слоты
- [ ] Возвращает 409 Conflict при попытке создания

---

#### P2.17. SlotRouter
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
API маршруты:
- GET `/cafes/{cafe_id}/slots`
- POST `/cafes/{cafe_id}/slots`
- PATCH `/cafes/{cafe_id}/slots/{slot_id}`
- DELETE `/cafes/{cafe_id}/slots/{slot_id}`

---

### BOOKING — Ответственный: Анастасия

#### P2.18. Модель Booking
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Модель Booking в `/src/app/models/booking.py`:
- user_id (FK), cafe_id (FK), table_id (FK), slot_id (FK)
- date (date), status (enum: new/confirmed/cancelled/finished)
- note, remind_at, timestamps, active
- Уникальный индекс на (cafe_id, table_id, slot_id, date)

**Acceptance Criteria:**
- [ ] Статус — это Enum
- [ ] Все FK настроены
- [ ] Индексы для быстрых запросов

---

#### P2.19. BookingRepository
**Срок:** 1 день  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
CRUD для броней с проверками:
- `get_available_slots()` — доступные слоты в кафе на дату
- `check_table_availability()` — занят ли столик на слот/дату
- `get_user_bookings()` — брони пользователя
- `get_cafe_bookings()` — брони кафе

**Acceptance Criteria:**
- [ ] `get_available_slots()` возвращает только свободные слоты
- [ ] `check_table_availability()` проверяет (cafe_id, table_id, slot_id, date)
- [ ] Все методы async

---

#### P2.20. BookingService
**Срок:** 1–1.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Сложная бизнес-логика:
- `create_booking()` — создание с проверками:
  - дата не в прошлом
  - столик свободен
  - пользователь не забронировал ещё одно место на это время
  - кафе и слот активны
- `update_booking_status()` — изменение статуса (new → confirmed → finished или cancelled)
- `cancel_booking()` — отмена с логированием
- Уведомление пользователю при создании (отложенное, для Celery)

**Acceptance Criteria:**
- [ ] Все проверки реализованы
- [ ] Бронь создаётся атомарно (нет race condition)
- [ ] Логируются все важные действия
- [ ] В базе устанавливается `remind_at` для Celery

---

#### P2.21. BookingRouter
**Срок:** 1 день  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
API маршруты:
- POST `/booking` — создать бронь
- GET `/booking/{id}` — детали брони
- GET `/booking` — мои брони (с фильтром по статусу)
- PATCH `/booking/{id}` — обновить статус
- DELETE `/booking/{id}` — отменить бронь

**Acceptance Criteria:**
- [ ] Только владелец брони может смотреть/менять свою бронь
- [ ] Менеджер кафе может менять статус броней своего кафе
- [ ] Возвращает 403 если нет прав
- [ ] Документировано в Swagger

---

### IMAGES / UPLOAD — Ответственный: Данил + Лев

#### P2.22. Модель Media
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Модель Media в `/src/app/models/media.py`:
- id (UUID4), file_path, mime_type, file_size, timestamps, active

**Acceptance Criteria:**
- [ ] id — UUID4 с default=uuid.uuid4
- [ ] file_path уникален
- [ ] mime_type = "image/jpeg" или "image/png"

---

#### P2.23. UploadService
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
Сервис загрузки в `/src/app/services/media_service.py`:
- Валидация: только JPG/PNG, ≤5MB
- Конвертация в JPG если нужно (Pillow)
- Сохранение в `/app/media/{uuid}.jpg`
- Создание записи в БД

**Acceptance Criteria:**
- [ ] Только изображения загружаются
- [ ] Размер проверяется на клиенте и сервере
- [ ] Файл сохраняется с UUID именем
- [ ] Возвращается Media объект

---

#### P2.24. MediaRouter
**Срок:** 0.5 дня  
**Метка:** `P2`, `backend`, `feature`

**Описание:**  
API маршруты:
- POST `/media/upload` — загрузить изображение
- GET `/media/{id}` — получить изображение (бинарный JPG)
- DELETE `/media/{id}` — удалить изображение (админ)

**Acceptance Criteria:**
- [ ] POST возвращает 201 и Media объект
- [ ] GET возвращает бинарный файл с правильным Content-Type
- [ ] Изображение доступно сразу после загрузки

---

#### P2.25. Главный router — объединение всех модулей
**Ответственный:** Данил  
**Срок:** 2 часа  
**Метка:** `P2`, `backend`, `task`

**Описание:**  
В `/src/app/main.py` объединить все роутеры:

```python
from fastapi import FastAPI
from app.api.v1.users import router as users_router
from app.api.v1.cafes import router as cafes_router
# ... остальные

app = FastAPI()
app.include_router(users_router, prefix="/api/v1")
app.include_router(cafes_router, prefix="/api/v1")
# ... остальные
```

**Acceptance Criteria:**
- [ ] Все роутеры подключены
- [ ] `/docs` показывает все эндпоинты
- [ ] Приложение стартует без ошибок

---

## 🟨 P3 — Celery, уведомления, оптимизации

### P3.1. Celery init + Celery worker
**Ответственный:** Андрей  
**Срок:** 1 день  
**Метка:** `P3`, `backend`, `feature`

**Описание:**  
Инициализировать Celery в `/src/app/core/celery_app.py`:

```python
from celery import Celery

celery_app = Celery(
    "booking",
    broker=settings.RABBITMQ_URL,
    backend=settings.REDIS_URL,
)

celery_app.autodiscover_tasks(["app.core.celery_tasks"])
```

Создать worker в docker-compose.

**Acceptance Criteria:**
- [ ] `celery -A app.core.celery_app worker --loglevel=info` запускается
- [ ] Flower доступен на `http://localhost:5555`

---

### P3.2. Создание Celery задач: напоминание о бронировании
**Ответственный:** Андрей  
**Срок:** 1 день  
**Метка:** `P3`, `backend`, `feature`

**Описание:**  
Создать задачи в `/src/app/core/celery_tasks.py`:
- `send_booking_reminder` — отправить напоминание за 1 час до бронирования
- Использовать Celery Beat для периодических задач

**Acceptance Criteria:**
- [ ] Задача выполняется в нужное время
- [ ] Логирование всех отправок
- [ ] Можно проверить в Flower

---

### P3.3. Обработка отмены бронирования
**Срок:** 0.5 дня  
**Метка:** `P3`, `backend`, `feature`

**Описание:**  
Создать Celery задачу для отправки уведомления при отмене.

---

### P3.4. Кэширование: Redis (опционально)
**Ответственный:** Лев  
**Срок:** 0.5 дня  
**Метка:** `P3`, `backend`, `feature`

**Описание:**  
Кэшировать в Redis:
- Список кафе
- Доступные слоты (с TTL 5 минут)

---

### P3.5. Улучшенная валидация времени
**Срок:** 0.5 дня  
**Метка:** `P3`, `backend`, `feature`

**Описание:**  
Добавить проверки времени:
- Нельзя бронировать на прошедшую дату
- Нельзя бронировать на очень близкое время (например, < 30 минут)

---

## 🟩 P4 — Опциональные улучшения

### P4.1. Dishes / Actions (Блюда и Акции)
**Ответственные:** По желанию  
**Срок:** 1–2 дня  
**Метка:** `P4`, `backend`, `feature`

**Описание:**  
Реализовать модели, CRUD, API для:
- Dishes (блюда в меню кафе)
- Actions (акции и специальные предложения)
- Связи many-to-many

---

### P4.2. Админ-эндпоинты
**Срок:** 1 день  
**Метка:** `P4`, `backend`, `feature`

**Описание:**  
Создать админ API для:
- Управления пользователями (блокировка, удаление)
- Статистики (кол-во броней, активные пользователи)
- Управления кафе (создание, удаление)

---

### P4.3. Документация (Swagger тюнинг)
**Срок:** 0.5 дня  
**Метка:** `P4`, `backend`, `docs`

**Описание:**  
Улучшить документацию в Swagger:
- Примеры запросов/ответов
- Описания статус кодов
- Группировка по тегам

---

## 📋 SUMMARY — Как добавить эти задачи на GitHub

1. Перейти в репозиторий → Issues → New Issue
2. Для каждой задачи:
   - **Title:** `[P1][Feature Name] Description`
   - **Body:** Скопировать описание из этого файла
   - **Labels:** P1/P2/P3/P4, backend, feature/task/bug
   - **Assignees:** Назначить ответственного
   - **Milestone:** "MVP 1.0" или "Celery & Notifications"

3. Или использовать GitHub CLI:
```bash
gh issue create \
  --title "[P2][Users] Create User models" \
  --body "Description here" \
  --label "P2,backend,feature" \
  --assignee "alexander"
```

---

## 📊 TIMELINE (Ориентировочно)

| Этап | Задачи | Дней |
|------|--------|------|
| **P1** | База проекта | 3–4 дня |
| **P2.Users** | Auth + Users | 3–4 дня |
| **P2.Cafes+Tables** | Cafes + Tables | 3 дня |
| **P2.Slots** | Slots + валидация | 2 дня |
| **P2.Booking** | Booking + сложная логика | 4–5 дней |
| **P2.Media** | Upload images | 1–2 дня |
| **Integrация** | Главный router, тестирование | 2 дня |
| **P3** | Celery + уведомления | 2–3 дня |
| **P4** | Опциональные улучшения | По времени |
| **ИТОГО** | MVP | 20–25 дней |

---

## ✅ ACCEPTANCE CRITERIA — Общее для всех задач

Каждая задача P2+ должна иметь:
- [ ] Код написан и протестирован локально
- [ ] Коммит с описательным сообщением
- [ ] Pull Request в develop с описанием изменений
- [ ] Code Review пройден (тимлид)
- [ ] Всё объединено в develop
- [ ] В Swagger видны все эндпоинты
- [ ] Логирование работает

---

**Готовай, команда! Поехали! 🚀**
